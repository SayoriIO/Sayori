[build]
builder = "nixpacks"
buildCommand = "PIPENV_VENV_IN_PROJECT=1 pipenv install --deploy"

[deploy]
startCommand = "python webserver_api.py"
healthcheckPath = "/"
healthcheckTimeout = 2000
restartPolicyType = "never"